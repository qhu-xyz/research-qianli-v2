# adjust baseline
## DA hourly
- DA hourly shadow constraint get rid of top 5, use this as constraints shadow
- use shift factor to stitch together a path's DA

## how to examin/cherry pick the path pool
- stitch together my annual spice signal and calculate the path's revenue. Assume that the tier-0 paths are the paths we pick the most.